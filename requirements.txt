# Dev install: the package plus every optional dependency group.
# Dependencies (with exact pins) are declared in pyproject.toml.
#
#   python -m venv .venv
#   .venv\Scripts\pip install -r requirements.txt
#
# For a smaller install, pick groups directly instead, e.g.:
#   pip install -e ".[camera,sdk]"     # real hardware, no harness
#   pip install -e .                   # mock only

-e .[all]
